[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
log_cli = true
log_cli_level = WARNING
markers =
    unit: Pure unit tests (no external services)
    integration: Integration tests (may mock Kafka/LiveKit)
    slow: Tests that take more than 5 seconds
